{% schema %}
{
  "name": "Commitment to Reliability",
  "tag": "section",
  "class": "section",
  "settings": [
     {
      "type": "image_picker",
      "id": "image",
      "label": "Image (Upload)"
    },
    {
       "type": "text",
       "id": "image_filename",
       "label": "Image Filename (Asset)",
       "info": "Enter filename like 'reliability.svg' to use from assets.",
       "default": "reliability.svg"
    },
    {
      "type": "text",
      "id": "heading",
      "label": "Heading",
      "default": "Commitment to Reliability & Stock Consistency"
    },
    {
      "type": "richtext",
      "id": "content",
      "label": "Content",
      "default": "<p>In hospitality, delays and stock shortages can impact service quality. That’s why we prioritize:</p><p>Consistent inventory availability<br>Strong supplier relationships<br>Efficient order processing</p><ul><li>Reliable delivery timelines</li><li>We work proactively to close supply gaps and reduce disruptions so your operations stay uninterrupted.</li></ul>"
    },
    {
       "type": "color",
       "id": "bg_color",
       "label": "Background Color",
       "default": "#ffffff"
    },
    {
      "type": "range",
      "id": "padding_top",
      "min": 0,
      "max": 100,
      "step": 4,
      "unit": "px",
      "label": "Top Padding",
      "default": 60
    },
    {
      "type": "range",
      "id": "padding_bottom",
      "min": 0,
      "max": 100,
      "step": 4,
      "unit": "px",
      "label": "Bottom Padding",
      "default": 60
    }
  ],
  "presets": [
    {
      "name": "Commitment to Reliability"
    }
  ]
}
{% endschema %}

{% style %}
  @import url('https://fonts.googleapis.com/css2?family=Rethink+Sans:wght@400;500;600;700&display=swap');

  .section-{{ section.id }}-padding {
    padding-top: {{ section.settings.padding_top | times: 0.75 | round: 0 }}px;
    padding-bottom: {{ section.settings.padding_bottom | times: 0.75 | round: 0 }}px;
    background-color: {{ section.settings.bg_color }};
  }

  @media screen and (min-width: 750px) {
    .section-{{ section.id }}-padding {
      padding-top: {{ section.settings.padding_top }}px;
      padding-bottom: {{ section.settings.padding_bottom }}px;
    }
  }

  .reliability-wrapper {
    max-width: 1400px; /* Matching the Canada section width */
    margin: 0 auto;
    padding: 0 2rem;
    font-family: 'Rethink Sans', sans-serif;
    display: flex;
    align-items: center;
    gap: 80px;
    flex-direction: row; /* Default: Text Left, Image Right */
  }

  .reliability-content {
    flex: 1;
    text-align: left;
    padding-right: 20px;
  }

  .reliability-heading {
    font-family: 'Rethink Sans', sans-serif !important;
    font-size: 42px;
    font-weight: 700;
    margin-top: 0;
    margin-bottom: 24px;
    color: #000000;
    line-height: 1.1;
    letter-spacing: -0.02em;
  }

  .reliability-text {
    font-family: 'Rethink Sans', sans-serif !important;
    font-size: 16px;
    line-height: 1.6;
    color: #121212;
    font-weight: 400;
  }
  
  .reliability-text p {
      margin-top: 0;
      margin-bottom: 24px;
  }
  
  .reliability-text ul {
      margin-top: 10px;
      margin-bottom: 24px;
      padding-left: 20px;
  }
  
  .reliability-text li {
      margin-bottom: 8px;
  }

  .reliability-image-container {
    flex: 1;
    display: flex;
    justify-content: center;
    align-items: center;
  }

  .reliability-image {
    width: 100%;
    height: auto;
    border-radius: 12px;
    object-fit: contain;
    display: block;
    max-width: 100%;
  }

  .reliability-image[src*=".svg"] {
      width: 100%;
  }

  .reliability-image-placeholder {
      width: 100%;
      background: #f0f0f0;
      min-height: 400px;
      display: flex;
      align-items: center;
      justify-content: center;
      color: #888;
      border-radius: 12px;
  }

  @media screen and (max-width: 990px) {
    .reliability-heading {
        font-size: 32px;
    }
    .reliability-wrapper {
        gap: 40px;
    }
  }

  @media screen and (max-width: 749px) {
    .reliability-wrapper {
        flex-direction: column-reverse; /* Image Top, Text Bottom */
        padding: 0 1.5rem;
        gap: 32px;
    }
    
    .reliability-image-container, .reliability-content {
        width: 100%;
        padding-right: 0;
    }

    .reliability-heading {
        font-size: 28px;
        margin-bottom: 16px;
    }
  }
{% endstyle %}

<div class="section-{{ section.id }}-padding">
  <div class="reliability-wrapper">
    <div class="reliability-content">
      {%- if section.settings.heading != blank -%}
        <h2 class="reliability-heading">{{ section.settings.heading | escape }}</h2>
      {%- endif -%}
      
      {%- if section.settings.content != blank -%}
        <div class="reliability-text">
          {{ section.settings.content }}
        </div>
      {%- endif -%}
    </div>

    <div class="reliability-image-container">
      {%- if section.settings.image_filename != blank -%}
         <img
            src="{{ section.settings.image_filename | asset_url }}"
            alt="{{ section.settings.heading | escape }}"
            class="reliability-image"
            loading="lazy"
            width="600"
            height="auto"
          >
      {%- elsif section.settings.image != blank -%}
        {{ section.settings.image | image_url: width: 800 | image_tag: 
          class: 'reliability-image',
          loading: 'lazy',
          width: section.settings.image.width,
          height: section.settings.image.height
        }}
      {%- else -%}
        <div class="reliability-image-placeholder">
            {{ 'image' | placeholder_svg_tag: 'placeholder-svg' }}
        </div>
      {%- endif -%}
    </div>
  </div>
</div>
